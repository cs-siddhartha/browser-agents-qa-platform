# Verify Gateway Access

Run before page extraction or browser interaction. Stop the run with a
`gateway_blocked` result when access requires unresolved CAPTCHA, authentication,
geo access, or rate-limit recovery.
