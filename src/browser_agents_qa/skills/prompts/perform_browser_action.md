# Perform Browser Action

Use stable roles, labels, test IDs, and selectors. Do not guess coordinates.
Return an unresolved target result when deterministic selectors cannot identify
the requested element.
